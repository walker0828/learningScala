package org.example.oneDay

import OneModule.abs
class TestOne {
  def generateNrandowm(n : Int): Unit = {
    val nums = new Array[Int](10);

    abs(1);
//    for ( i <- until nums.length) {
//
//    }
  }

  def m1(x: Int) = x * x;


}
